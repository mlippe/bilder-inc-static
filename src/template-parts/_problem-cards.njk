{% macro el(isHome,title) %}
  <div class="problem-cards scroll-animation">
    <div class="layout">
      <div class="inner grid-max">
        <h2>{{ title }}</h2>
        <div class="card-container">
          <div class="card inner-delay level-2">
            {% if isHome %}
              <img src="assets/img/problem-cards/clutter.svg"/>
            {% else %}
              <img src="../assets/img/problem-cards/clutter.svg"/>
            {% endif %}
            <div class="title">Eliminate clutter</div>
            <div class="desc">Many photo libraries are distributed among different types of media: on phones, computers, hard drives
              or event prints. Specific photos aren’t found quickly this way. The centralised nature of BilderBox helps with this.</div>
          </div>
          <div class="card inner-delay level-3">
            {% if isHome %}
              <img src="assets/img/problem-cards/spiderweb.svg"/>
            {% else %}
              <img src="../assets/img/problem-cards/spiderweb.svg"/>
            {% endif %}
            <div class="title">Bring photos back to life</div>
            <div class="desc">We take increasingly more photos every day. Yet we tend to not look at them very often. BilderBook and
              BilderBeam enhance the consumption of your memories: enjoy personalised photo curations!</div>
          </div>
          <div class="card inner-delay level-4">
            {% if isHome %}
              <img src="assets/img/problem-cards/safe.svg"/>
            {% else %}
              <img src="../assets/img/problem-cards/safe.svg"/>
            {% endif %}
            <div class="title">Secure<br>& Private</div>
              <div class="desc">Photos are physical reminders of precious memories. We at Bilder Inc. believe that you should be in
                control of your privacy. The Bilder system never loads any data to the cloud. All photos stay local in your BilderBox.
                This also applies to our AI software.</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  {% endmacro %}